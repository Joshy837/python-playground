{
  "steps": [
    {
      "title": "Your First Variable",
      "example": "name = \"Maya\"\nprint(name)\n\n# Try changing \"Maya\" to your own name and running again",
      "task": "Create a variable called `name` and set it to your name (or any name you like).",
      "starter": "name = \"\"\nprint(name)\n",
      "quiz": [
        {
          "question": "Which line creates a variable called name?",
          "options": ["print(name)", "name = \"Maya\"", "\"Maya\"", "name"],
          "answer": 1,
          "explanation": "The = sign stores the value on the right into the label on the left."
        },
        {
          "question": "What does the = sign do in: name = \"Maya\"?",
          "options": ["Checks if name equals \"Maya\"", "Stores \"Maya\" into the variable name", "Prints \"Maya\" to the screen", "Deletes the variable name"],
          "answer": 1,
          "explanation": "= is the assignment operator — it stores a value into a variable."
        },
        {
          "question": "After running name = \"Alex\", what is stored in name?",
          "options": ["name", "=", "\"Alex\"", "Nothing"],
          "answer": 2,
          "explanation": "The value on the right of = is what gets stored in the variable."
        },
        {
          "question": "To display a variable's value on screen, we use:",
          "options": ["display(name)", "show(name)", "print(name)", "output(name)"],
          "answer": 2,
          "explanation": "print() is Python's built-in function for showing output."
        },
        {
          "question": "Complete the line: _____(name) shows the value of name on screen.",
          "answer": ["print"],
          "explanation": "print() is Python's built-in function for displaying output."
        }
      ],
      "tests": [
        {
          "name": "name is a non-empty string",
          "check": "isinstance(name, str) and len(name) > 0",
          "msg": "name should be a string with at least one character — wrap it in quotes"
        }
      ],
      "description": "# Your First Variable\n\nImagine you have a box with a label on it that says **\"name\"**. You put the word **\"Maya\"** inside the box.\n\nIn Python, that's exactly what a **variable** is — a labelled box that stores a value!\n\n```python\nname = \"Maya\"\nprint(name)\n```\n\n| Part | What it means |\n|------|---------------|\n| `name` | The label on the box — you choose this name! |\n| `=` | \"Put this value inside the box\" — an instruction, not a question |\n| `\"Maya\"` | The value stored inside — quote marks mean it's text |\n| `print(name)` | \"Open the box called `name` and show me what's inside!\" |\n\nTry changing `\"Maya\"` to your own name and press Run — you should see your name appear!"
    },
    {
      "title": "Strings",
      "example": "pet = \"Fluffy\"\ncity = 'London'\nprint(pet)\nprint(city)",
      "task": "Create a variable called `favourite_food` and set it to your favourite food as a string.",
      "starter": "favourite_food = \"\"\nprint(favourite_food)\n",
      "quiz": [
        {
          "question": "Which of these is a string?",
          "options": ["42", "True", "\"hello\"", "3.14"],
          "answer": 2,
          "explanation": "Strings are text values wrapped in quote marks."
        },
        {
          "question": "Which is also a valid way to write a string in Python?",
          "options": ["Both \"hello\" and 'hello' work", "Only \"hello\" works", "Only 'hello' works", "Neither — strings need backticks"],
          "answer": 0,
          "explanation": "Python accepts both single and double quotes for strings."
        },
        {
          "question": "What happens if you write pet = Fluffy without any quote marks?",
          "options": ["Python treats Fluffy as text", "Python looks for a variable called Fluffy", "Python creates the string automatically", "Nothing changes"],
          "answer": 1,
          "explanation": "Without quotes, Python thinks Fluffy is the name of another variable."
        },
        {
          "question": "Which of these is NOT a string?",
          "options": ["\"42\"", "'hello'", "42", "\"True\""],
          "answer": 2,
          "explanation": "42 without quotes is an integer, not a string."
        },
        {
          "question": "A piece of text stored in Python is called a _____.",
          "answer": ["string"],
          "explanation": "Any value wrapped in quote marks is called a string."
        }
      ],
      "tests": [
        {
          "name": "favourite_food is a non-empty string",
          "check": "isinstance(favourite_food, str) and len(favourite_food) > 0",
          "msg": "favourite_food should be a string — wrap it in quotes"
        }
      ],
      "description": "# Text Values — Strings\n\nWhenever you want to store words or sentences in Python, you use something called a **string**.\n\nThe rule is simple: **always wrap your text in quote marks**.\n\n```python\npet = \"Fluffy\"\ncity = 'London'\n```\n\nYou can use double quotes `\"\"` or single quotes `''` — they both do the same thing!\n\nWhy do we need the quotes? Without them, Python would think `Fluffy` is the name of a variable, not a piece of text. The quotes say: \"hey Python, this is just text!\"\n\nStrings can hold names, sentences, emojis — anything made of characters:\n```python\nmessage = \"Hello, world!\"\nanimal = \"🐶\"\n```\n\nTry creating your own string and printing it!"
    },
    {
      "title": "Joining Strings",
      "example": "first_name = \"Maya\"\nlast_name = \"Smith\"\nfull_name = first_name + \" \" + last_name\nprint(full_name)",
      "task": "You have `first = \"Hello\"` and `name = \"Maya\"`. Create a variable `greeting` that joins them with a space between — it should equal `\"Hello Maya\"`.",
      "starter": "first = \"Hello\"\nname = \"Maya\"\ngreeting = \"\"\n",
      "quiz": [
        {
          "question": "What does \"Hello, \" + \"world\" produce?",
          "options": ["An error", "\"Hello, \" (just the first part)", "\"Hello, world\"", "\"Helloworld\""],
          "answer": 2,
          "explanation": "+ joins two strings together into one."
        },
        {
          "question": "What does \"abc\" + \"def\" equal?",
          "options": ["\"abcdef\"", "\"abc def\"", "\"def abc\"", "An error"],
          "answer": 0,
          "explanation": "+ joins the strings directly, with no space added automatically."
        },
        {
          "question": "What is \"Hi\" + \" \" + \"there\"?",
          "options": ["\"Hi there\"", "\"Hithere\"", "\"Hi  there\"", "An error"],
          "answer": 0,
          "explanation": "The middle \" \" adds the space between \"Hi\" and \"there\"."
        },
        {
          "question": "How would you join first = \"Hello\" and name = \"Maya\" with a space between?",
          "options": ["first name", "first + name", "first + \" \" + name", "first, name"],
          "answer": 2,
          "explanation": "You must include the space explicitly as its own string between the two."
        },
        {
          "question": "The symbol used to join two strings together is _____.",
          "answer": ["+"],
          "explanation": "+ combines two strings into one longer string."
        }
      ],
      "tests": [
        {
          "name": "greeting == \"Hello Maya\"",
          "check": "greeting == \"Hello Maya\"",
          "msg": "greeting should be \"Hello Maya\" — join first, a space \" \", and name with +"
        }
      ],
      "description": "# Joining Strings\n\nImagine you have two pieces of paper, each with a word written on it. You can glue them together to make one longer piece. In Python, we do the same thing with `+`!\n\n```python\nfirst = \"Hello, \"\nsecond = \"Maya!\"\nmessage = first + second\nprint(message)\n```\n\nOutput: `Hello, Maya!`\n\nImportant thing to notice: there's a space inside `\"Hello, \"` at the end. If we left it out:\n```python\n\"Hello,\" + \"Maya!\"  # gives \"Hello,Maya!\" — no space!\n```\n\nSo if you want a gap between words, you need to add it yourself — either at the end of one string, or by joining a space in between:\n```python\nfull_name = \"Maya\" + \" \" + \"Smith\"  # \"Maya Smith\"\n```\n\nTry joining your first name and last name together!"
    },
    {
      "title": "Integers",
      "example": "age = 10\nprint(age)\n\ncoins = 25\nprint(coins)",
      "task": "Create a variable called `age` and set it to any whole number.",
      "starter": "age = 0\nprint(age)\n",
      "quiz": [
        {
          "question": "Which stores a whole number (integer)?",
          "options": ["age = \"10\"", "age = 10", "age = 10.0", "age = True"],
          "answer": 1,
          "explanation": "Integers are whole numbers — no quotes and no decimal point."
        },
        {
          "question": "Which of these is NOT an integer?",
          "options": ["0", "-5", "100", "3.5"],
          "answer": 3,
          "explanation": "3.5 has a decimal point, so it's a float, not an integer."
        },
        {
          "question": "What does Python treat age = \"10\" as?",
          "options": ["An integer", "A float", "A string", "A boolean"],
          "answer": 2,
          "explanation": "The quotes make it a string — Python would not treat it as a number."
        },
        {
          "question": "Can integers be negative?",
          "options": ["No, integers are always positive", "Yes, like -7 or -100", "Only if you use abs()", "Only floats can be negative"],
          "answer": 1,
          "explanation": "Integers can be negative — just add a minus sign in front."
        },
        {
          "question": "Whole numbers with no decimal point are called _____s in Python.",
          "answer": ["integer", "int"],
          "explanation": "The full name is 'integer', and Python's short name is 'int'."
        }
      ],
      "tests": [
        {
          "name": "age is an integer",
          "check": "isinstance(age, int) and not isinstance(age, bool)",
          "msg": "age should be a whole number — no quotes, no decimal point"
        }
      ],
      "description": "# Whole Numbers — Integers\n\nNot everything we store is text! Sometimes we want to store a **number** — like someone's age, a score, or how many coins you have.\n\nNumbers in Python don't need quote marks. Just write the number directly:\n\n```python\nage = 10\ncoins = 25\nprint(age)\nprint(coins)\n```\n\nThese are called **integers** — whole numbers with no decimal point (no `.5` or `.99`).\n\nWatch out: if you put quotes around a number, Python treats it as text, not a number!\n```python\nage = \"10\"   # this is text — you can't do maths with it\nage = 10     # this is a number — you can!\n```\n\nIntegers can be negative too:\n```python\ntemperature = -5\n```"
    },
    {
      "title": "Maths",
      "example": "apples = 5\noranges = 3\n\nprint(apples + oranges)\nprint(apples - oranges)\nprint(apples * 2)",
      "task": "You have `price = 5` and `quantity = 3`. Create a variable `total` that equals price multiplied by quantity.",
      "starter": "price = 5\nquantity = 3\ntotal = 0\n",
      "quiz": [
        {
          "question": "What does 4 * 3 equal in Python?",
          "options": ["7", "12", "43", "1"],
          "answer": 1,
          "explanation": "* is the multiplication operator in Python."
        },
        {
          "question": "Which symbol means multiply in Python?",
          "options": ["x", "×", "*", "·"],
          "answer": 2,
          "explanation": "Python uses * for multiplication because × isn't on standard keyboards."
        },
        {
          "question": "What does 10 / 2 give in Python?",
          "options": ["5", "5.0", "2", "10"],
          "answer": 1,
          "explanation": "/ always produces a float in Python 3 — even when the result is a whole number."
        },
        {
          "question": "What does 7 - 3 + 2 equal?",
          "options": ["6", "2", "12", "8"],
          "answer": 0,
          "explanation": "7 - 3 = 4, then 4 + 2 = 6."
        },
        {
          "question": "In Python, the symbol for multiplication is _____.",
          "answer": ["*"],
          "explanation": "* is used for multiplication (not x or ×)."
        }
      ],
      "tests": [
        {
          "name": "total == 15",
          "check": "total == 15",
          "msg": "5 * 3 = 15, so total should be 15"
        }
      ],
      "description": "# Maths with Numbers\n\nPython is like a super-powered calculator! You can do all kinds of maths using variables.\n\n| Operator | Means | Example |\n|----------|-------|---------|\n| `+` | add | `5 + 3` → `8` |\n| `-` | subtract | `5 - 3` → `2` |\n| `*` | multiply | `5 * 3` → `15` |\n| `/` | divide | `6 / 2` → `3.0` |\n\n```python\napples = 5\noranges = 3\n\nprint(apples + oranges)   # 8\nprint(apples - oranges)   # 2\nprint(apples * 2)         # 10\n```\n\nYou can also store the result in a new variable:\n```python\ntotal = apples + oranges\nprint(total)  # 8\n```\n\nNote: `*` is multiply (not `x`), and `/` is divide (not `÷`). Python uses these symbols because they're on every keyboard!\n\nTry changing the numbers and see how the results change."
    },
    {
      "title": "Floats",
      "example": "price = 1.99\nprint(price)\n\nhalf = 0.5\nprint(price + half)",
      "task": "Create a variable called `height` and set it to any decimal number (e.g. `1.5`).",
      "starter": "height = 0.0\nprint(height)\n",
      "quiz": [
        {
          "question": "Which of these is a float?",
          "options": ["5", "\"5.0\"", "5.0", "True"],
          "answer": 2,
          "explanation": "Any number written with a decimal point — like 5.0 or 1.99 — is a float."
        },
        {
          "question": "What makes a float different from an integer?",
          "options": ["It's always negative", "It has a decimal point", "It's in quotes", "It starts with f"],
          "answer": 1,
          "explanation": "The decimal point is what distinguishes a float from an integer."
        },
        {
          "question": "What is the result of 5 + 1.5 in Python?",
          "options": ["6", "6.5", "\"6.5\"", "An error"],
          "answer": 1,
          "explanation": "Python can add integers and floats together — the result is a float."
        },
        {
          "question": "Is 1.0 a float or an integer?",
          "options": ["Integer, because the decimal part is 0", "Float, because it has a decimal point", "Both", "Neither — it's invalid"],
          "answer": 1,
          "explanation": "Any number with a decimal point is a float, even if the decimal part is zero."
        },
        {
          "question": "Numbers with a decimal point in Python are called _____s.",
          "answer": ["float"],
          "explanation": "Float is short for 'floating point number'."
        }
      ],
      "tests": [
        {
          "name": "height is a positive float",
          "check": "isinstance(height, float) and height > 0",
          "msg": "height should be a float greater than 0 — include a decimal point (e.g. 1.5)"
        }
      ],
      "description": "# Decimal Numbers — Floats\n\nSometimes whole numbers aren't enough. A price like `£1.99`, a height like `1.75 m`, or a temperature like `36.6°C` all need a decimal point.\n\nIn Python, numbers with a decimal point are called **floats** (short for \"floating point number\"):\n\n```python\nprice = 1.99\nheight = 1.75\ntemperature = 36.6\n```\n\nYou can do maths with floats just like integers:\n```python\nprice = 1.99\ndiscount = 0.50\nfinal = price - discount\nprint(final)  # 1.49\n```\n\nEven `1.0` is a float — the decimal point is what tells Python it's a float, not an integer.\n\nFloats and integers can work together:\n```python\nprint(5 + 1.5)  # 6.5\n```"
    },
    {
      "title": "Booleans",
      "example": "is_sunny = True\nhas_pet = False\n\nprint(is_sunny)\nprint(has_pet)",
      "task": "Create a variable called `is_raining` and set it to either `True` or `False`.",
      "starter": "is_raining = False\nprint(is_raining)\n",
      "quiz": [
        {
          "question": "Which is a valid Python boolean?",
          "options": ["\"True\"", "true", "True", "1"],
          "answer": 2,
          "explanation": "Python booleans are True or False — capital first letter, no quotes."
        },
        {
          "question": "What are the only two values a boolean can have?",
          "options": ["Yes / No", "1 / 0", "True / False", "On / Off"],
          "answer": 2,
          "explanation": "Booleans are always either True or False."
        },
        {
          "question": "What is wrong with: is_raining = true?",
          "options": ["Nothing is wrong", "true should be True (capital T)", "true should be in quotes", "Booleans need == not ="],
          "answer": 1,
          "explanation": "Python booleans must start with a capital letter: True or False."
        },
        {
          "question": "What type does Python return when you compare two values, like 5 > 3?",
          "options": ["integer", "string", "float", "boolean"],
          "answer": 3,
          "explanation": "Comparisons always return True or False — both are booleans."
        },
        {
          "question": "The two boolean values in Python are True and _____.",
          "answer": ["False"],
          "explanation": "Booleans are always either True or False — both need a capital first letter."
        }
      ],
      "tests": [
        {
          "name": "is_raining is a boolean",
          "check": "isinstance(is_raining, bool)",
          "msg": "is_raining should be True or False (capital T/F, no quotes)"
        }
      ],
      "description": "# Yes and No — Booleans\n\nSometimes we just need to answer a yes/no question: Is it raining? Does the player have a key? Is the game over?\n\nFor that, Python has a special type called a **boolean**. It can only ever be one of two values:\n- `True` — yes!\n- `False` — no!\n\n```python\nis_sunny = True\nhas_pet = False\nprint(is_sunny)   # True\nprint(has_pet)    # False\n```\n\nTwo things to remember:\n1. The first letter is **capital** — `True` not `true`, `False` not `false`\n2. No quote marks — `True` is a special keyword, `\"True\"` is just the text \"True\"\n\nBooleans might seem simple now, but they're really powerful! When we get to `if` statements, we'll use them to make decisions in our code:\n```python\nif is_sunny:\n    print(\"Let's go outside!\")\n```"
    }
  ]
}
